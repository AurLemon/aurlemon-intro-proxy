{
  "name": "github-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for GitHub OAuth and contributions endpoints.",
  "main": "src/worker.js",
  "type": "module",
  "scripts": {
    "cf:dev": "wrangler dev",
    "cf:deploy": "wrangler deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.114.15"
  }
}
