{
  "name": "aurlemon-intro-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for AurLemon Intro upstream API requests.",
  "main": "src/worker.js",
  "type": "module",
  "scripts": {
    "cf:dev": "wrangler dev",
    "cf:deploy": "wrangler deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.114.15"
  }
}
